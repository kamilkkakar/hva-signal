/**
 * Public Decision UI 2.0 route.
 * Must not import fixtures or TEST_ONLY values.
 */
import { ProductShell } from "@/components/shell/ProductShell";

export function App() {
  return <ProductShell />;
}
