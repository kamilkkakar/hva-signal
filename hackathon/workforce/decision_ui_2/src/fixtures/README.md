# TEST_ONLY fixtures

These files exist for component tests only.

Do not import them from `App.tsx`, `ProductShell.tsx`, or `publicSurface.ts`.

Do not display fixture magnitudes on the public decision surface until the temporal program publishes real contracts.
